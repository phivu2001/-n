@{
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}
@model DTO.Vendor.VendorDTO
<div class="account-management">
    @Html.Partial("_breadcrumb", new Dictionary<string, string>
        { { "Home", "Home" }, { "VendorManagement", "Vendor Management" }, { "C", "Add new" } }, new ViewDataDictionary(ViewData) { { "mHref", Url.Action("Index", "VendorManagement") }, { "mShowButton", "false" } })

        @using (Html.BeginForm("New", "VendorManagement", FormMethod.Post, new { @id = "form-create", @class = "form-horizontal needs-validation", @enctype = "multipart/form-data" }))
        {
            @Html.Partial("_Form", Model)
        }
</div>

