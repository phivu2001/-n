@{
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}
@model DTO.Warehouse.OutWarehouseDTO
<div class="account-management">
    @Html.Partial("_breadcrumb", new Dictionary<string, string>
        { { "Home", "Home" }, { "OutWarehouseManagement", "Quản lý nhập kho" }, { "C", "Add new" } }, new ViewDataDictionary(ViewData) { { "mHref", Url.Action("Index", "OutWarehouseManagement") }, { "mShowButton", "false" } })

        @using (Html.BeginForm("New", "OutWarehouseManagement", FormMethod.Post, new { @id = "form-create", @class = "form-horizontal needs-validation", @enctype = "multipart/form-data" }))
        {
            @Html.Partial("_Form", Model)
        }
</div>

